using System;
using System.Collections.Generic;

namespace _03._10._2019
{
    class Program
    {
        static void Main(string[] args)
        {

        //    const string pathin = "textin.txt";
        //    const string pathout = "textout.txt";
        //    string str = System.IO.File.ReadAllText(pathin);
        //    List<string> bar = new List<string>();
        //    List<string> bar2 = new List<string>();
        //    char[] phraseAsChars = str.ToCharArray();
        //    bar.Add("netsenzur1");
        //    bar.Add("netsenzur2");
        //    string buf = "";
        //    int start=0;
        //    for (int i = 0; i < str.Length; i++)
        //    {
        //        start = i; 
        //        while(true)
        //        {
        //            if (i >= str.Length)
        //                break;
        //            if (str[i] == ' ' || str[i] == ',' || str[i] == '.' || str[i] == '!' || str[i] == ')' || str[i] == '(')
        //                break;
        //            buf += str[i];
        //            i++;
        //        }
        //        for (int j = 0; j < bar.Count; j++)
        //        {
        //            if(bar[j]==buf)
        //            {
                        
        //                for (int g = start; g < start+ buf.Length; g++)
        //                {

        //                    phraseAsChars[g] = '*';
        //                }
        //                break;
        //            }
        //        }
        //        start = 0;
        //        buf = "";
        //    }
        //    System.IO.File.WriteAllText(pathout, new string(phraseAsChars));
        }
    }
}
